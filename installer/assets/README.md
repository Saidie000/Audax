# Installer Assets

Place the official assets here:
- `app_icon.ico` (converted from the provided app icon)
- `banner.bmp` (493x58)
- `dialog.bmp` (493x312)
- `license.rtf`

These will be referenced by the WiX UI in `installer/wix/Product.wxs`.
