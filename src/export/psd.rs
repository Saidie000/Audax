//! PSD export module preserving layers.

use crate::export::ExportError;

pub fn export_psd(_path: &str) -> Result<(), ExportError> {
    // TODO: implement PSD export preserving layers and masks.
    Ok(())
}
